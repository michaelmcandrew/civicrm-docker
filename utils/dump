<?php

/**
 * Check that you have the environment variable necessary to create the dump
 */

// 'PROJECT_NAME'

/**
 * Create an array to describes the dump
 */



$state = [
    'created' => date(DATE_ISO8601),
    'databases' => [
        'gpie_civicrm',
        'gpie_drupal'
    ],
    'directories' => [
        '/var/www/html/sites/default/files'
    ]
];

echo json_encode($state, JSON_PRETTY_PRINT);
exit;


$url = parse_url('mysql://gpie:L8sLmhrRiooUZvQe@mysql/gpie_drupal?new_link=true');
// var_dump($url);
var_dump(getenv('MYSQL_ROOT_PASSWORD'));
